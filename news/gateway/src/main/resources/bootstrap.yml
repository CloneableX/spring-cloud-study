spring:
  cloud:
    config:
      name: eureka,gateway
      label: master
      discovery:
        enabled: true
        service-id: config
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/